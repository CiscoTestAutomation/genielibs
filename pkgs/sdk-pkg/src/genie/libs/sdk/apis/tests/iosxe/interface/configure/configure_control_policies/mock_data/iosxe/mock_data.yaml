configure:
  commands:
    1 authenticate using mab: "1 authenticate using mab\r\n"
    1 class always do-until-failure: "1 class always do-until-failure\r\n"
    end:
      new_state: execute
    event session-started match-all: "event session-started match-all\r\n"
    line console 0:
      new_state: configure_line
    no logging console: ''
    policy-map type control subscriber MAB: "policy-map type control subscriber MAB\r\
      \n"
  prompt: VCR(config)#
configure_line:
  commands:
    end:
      new_state: execute
    exec-timeout 0: ''
  prompt: VCR(config-line)#
connect:
  commands:
    ? ''
    : new_state: execute
  preface: 'Trying mock_device ...

    Connected to mock_device.

    Escape character is ''^]''.'
  prompt: ''
execute:
  commands:
    config term:
      new_state: configure
    config-transaction:
      new_state: configure
    show version: ''
    term length 0: ''
    term width 0: ''
  prompt: VCR#
