configure:
  commands:
    end:
      new_state: execute
    line console 0:
      new_state: configure_line
    no logging console: ''
    no snmp-server enable traps: "no snmp-server enable traps\r\n"
    no snmp-server engineID remote 30.30.30.2 800000090300005056BE0829: "no snmp-server\
      \ engineID remote 30.30.30.2 800000090300005056BE0829\r\n"
    no snmp-server host 30.30.30.2 informs version 3 priv privuser256256 config: "no\
      \ snmp-server host 30.30.30.2 informs version 3 priv privuser256256 config\r\
      \n"
    no snmp-server host 30.30.30.2 traps version 3 priv privuser256256 config: "no\
      \ snmp-server host 30.30.30.2 traps version 3 priv privuser256256 config\r\n"
    no snmp-server trap-source GigabitEthernet1: "no snmp-server trap-source GigabitEthernet1\r\
      \n"
  prompt: csr(config)#
configure_line:
  commands:
    end:
      new_state: execute
    exec-timeout 0: ''
  prompt: csr(config-line)#
connect:
  commands:
    ? ''
    : new_state: execute
  preface: 'Trying mock_device ...

    Connected to mock_device.

    Escape character is ''^]''.'
  prompt: ''
execute:
  commands:
    config term:
      new_state: configure
    config-transaction:
      new_state: configure
    show version: ''
    term length 0: ''
    term width 0: ''
  prompt: csr#
