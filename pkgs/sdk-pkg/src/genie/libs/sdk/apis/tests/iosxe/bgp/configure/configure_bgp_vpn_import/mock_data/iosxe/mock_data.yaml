configure:
  commands:
    address-family l2vpn evpn: "address-family l2vpn evpn\r\n"
    address-family vpnv4: "address-family vpnv4\r\n"
    end:
      new_state: execute
    import l2vpn evpn: "import l2vpn evpn\r\n"
    import vpnv4 unicast re-originate: "import vpnv4 unicast re-originate\r\n"
    line console 0:
      new_state: configure_line
    no logging console: ''
    router bgp 3: "router bgp 3\r\n"
  prompt: stack3-nyquist-1(config)#
configure_line:
  commands:
    end:
      new_state: execute
    exec-timeout 0: ''
  prompt: stack3-nyquist-1(config-line)#
connect:
  commands:
    ? ''
    : new_state: execute
  preface: 'Trying mock_device ...

    Connected to mock_device.

    Escape character is ''^]''.'
  prompt: ''
execute:
  commands:
    config term:
      new_state: configure
    config-transaction:
      new_state: configure
    show version: ''
    term length 0: ''
    term width 0: ''
  prompt: stack3-nyquist-1#
